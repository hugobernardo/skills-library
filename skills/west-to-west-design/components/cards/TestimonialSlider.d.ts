/**
 * Quote carousel for Ink sections: centered Sora quote in typographic quotes, circular arrows, green pill dot for active.
 */
export interface TestimonialSliderProps {
  /** Quote strings, shown one at a time (no attribution on the live site) */
  quotes: string[];
}
export declare function TestimonialSlider(props: TestimonialSliderProps): JSX.Element;
