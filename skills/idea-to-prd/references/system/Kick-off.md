Start a new product using the Product OS package.

Read README.md, AGENTS.md, and roles/PM.md. Follow the linked specialist and governance instructions when the process requires them.

Product name: [NAME]
Product directory: products/[NAME]/
Initial idea: [ROUGH DESCRIPTION]

Create the product directory and copy the Product_Memory.md template into it. Initialize the appropriate working artifacts, then begin PM discovery. During discovery, define the credible-MVP-to-v1-prod scope ladder, ask the product owner to select a build target, and agree a validation posture proportionate to the business objective. Do not create a PRD until the opportunity gate is met.

Keep all product-specific work inside this product directory. Maintain the canonical files and memory as decisions evolve. If this product already exists, read its memory and current artifacts and resume instead of reinitializing it.
